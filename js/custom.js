jQuery(document).ready(function ($) {

  // Do stuff here
  //console.log('custom js works');

  // break out of iframe if site is in one
  // (this is also to ensure break out of iframe after logging in with wp-login in iframe
  //if (window.location !== window.top.location) {
    //window.top.location = window.location;
  //}

}); // jQuery End
